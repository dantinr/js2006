<?php

    //购物车接口

    // 1获取 token
    $token = $_GET['token'];        // $_POST['token']   $_SERVER['HTTP_TOKEN']

    //验证token是否有效
    // TODO  select * from token where token = "{$token}"


    if(true)
    {
        // 返回购物车数据
    }else{
        //鉴权失败
    }


